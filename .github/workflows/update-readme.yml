name: Update README

on:
  workflow_dispatch:
  schedule:
    - cron: "0 2 * * *"

permissions:
  contents: write

jobs:
  update-readme:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Set up uv
        uses: astral-sh/setup-uv@v7
        with:
          python-version: "3.11"

      - name: Sync dependencies
        run: uv sync

      - name: Validate script
        run: uv run python -m py_compile scripts/scan_plugins.py

      - name: Regenerate reports
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: uv run python scripts/scan_plugins.py

      - name: Commit updated reports
        run: |
          if git diff --quiet -- README.md report.json; then
            echo "No report changes"
            exit 0
          fi

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add README.md report.json
          git commit -m "chore: update suspicious plugin reports"
          git push
